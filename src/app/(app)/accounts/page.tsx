import { AccountsIndex } from "@/features/accounts/accounts-index";

export const metadata = { title: "Accounts · Turc Global" };

export default function AccountsPage() {
  return <AccountsIndex />;
}
