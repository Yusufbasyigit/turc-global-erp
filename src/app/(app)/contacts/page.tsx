import { ContactsIndex } from "@/features/contacts/contacts-index";

export const metadata = { title: "Contacts · Turc Global" };

export default function ContactsPage() {
  return <ContactsIndex />;
}
